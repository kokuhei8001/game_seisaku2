using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunLv : MonoBehaviour {

    struct GunLevel {
        int Lv;
        float keikenti;
        float attck;
        float raet;
        float cool_time;
    };

    //GunLevel status = { 0, 0, 0, 0, 0 };


    public float AR = 0;
    public float LMG = 0;
    public float SG = 0;
    public float SR = 0;
   
	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {

    }
}
